import { Route } from "@angular/router";
import { ProductComponent } from "../Routing(Tejas)/product/product.component";

export const routes: Route[] = [
  {
    path: "product",
    component: ProductComponent
  }
];
